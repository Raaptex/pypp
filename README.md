# Py++
> Compiler for Py++ to Python or Executable
## Getting started
Compile command
```batch
py pypp.py -m {main.pypp} -o {out_file}
```
## Options
 - -m [main_file]
 - -o [out_file]
 - -b (Create an .exe)
 - -r (Auto run)
